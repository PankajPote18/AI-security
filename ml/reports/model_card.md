# Model Card — Phishing URL Classifier

_Generated by `copilot-ml train` on 2026-09-21, from the `phiusiil` dataset split (see `data_card.md`). Do not edit by hand; rebuild instead._

## 1. Selection rubric (fixed before results were seen)

Primary metric: **PR-AUC on the validation split**. Tie-breaks, in order: lower Brier score (calibration), lower prediction latency, smaller artifact, and simplicity (logistic regression before random forest before XGBoost). The champion is chosen **only among `host`-view candidates** - see §2.

## 2. Why the champion is a `host`-view model

The M1.1 data card's shortcut audit found every legitimate URL in this dataset is a bare `https://host` homepage, while phishing URLs commonly have paths/queries/plain http. A model trained on the `full` feature view (path, query, scheme included) can reach very high accuracy on this data by recognising *how the dataset was sourced* rather than *what phishing looks like*, which would not generalise to real traffic (a legitimate URL with a path is common in the real world; here there are none). The comparison table below reports both views side by side so this gap is visible, but only a `host`-view candidate can become the champion.

## 3. Candidate comparison (validation split, threshold 0.5, all 6 model x view combinations, ranked by the rubric)

| Model | View | PR-AUC | ROC-AUC | F1 | Brier | Predict latency | Artifact size |
|---|---|---|---|---|---|---|---|
| xgboost | full | 0.9994 | 0.9992 | 0.9982 | 0.0015 | 155.2 ms | 652 KB |
| random_forest | full | 0.9992 | 0.9990 | 0.9975 | 0.0025 | 438.0 ms | 14564 KB |
| logistic_regression | full | 0.9988 | 0.9985 | 0.9950 | 0.0036 | 39.6 ms | 6 KB |
| xgboost | host | 0.9413 | 0.9338 | 0.8587 | 0.0814 | 146.9 ms | 889 KB |
| random_forest | host | 0.9269 | 0.9190 | 0.8359 | 0.0966 | 463.6 ms | 56847 KB |
| logistic_regression | host | 0.8951 | 0.8746 | 0.8038 | 0.1168 | 32.2 ms | 5 KB |

## 4. Champion

**`xgboost` on the `host` view.** Validation PR-AUC 0.9413, ROC-AUC 0.9338.

Operating threshold: **0.3867** (probability of phishing at or above this is classified phishing), chosen on validation to maximise F1.

## 5. Final metrics at the chosen threshold

### Validation (used to pick the champion, threshold and this section's numbers)

| Metric | Value |
|---|---|
| Accuracy | 0.9033 |
| Precision (this data's ~43% prevalence) | 0.9440 |
| Precision (rebased to 1.00% prevalence) | 0.1859 |
| Recall | 0.8224 |
| F1 | 0.8790 |
| ROC-AUC | 0.9338 |
| PR-AUC | 0.9413 |
| Brier score | 0.0814 |
| Confusion matrix (tn, fp, fn, tp) | 19493, 736, 2678, 12400 |

### Test (touched exactly once, after every other decision was made)

| Metric | Value |
|---|---|
| Accuracy | 0.8877 |
| Precision (this data's ~43% prevalence) | 0.9431 |
| Precision (rebased to 1.00% prevalence) | 0.1833 |
| Recall | 0.7845 |
| F1 | 0.8565 |
| ROC-AUC | 0.9240 |
| PR-AUC | 0.9322 |
| Brier score | 0.0874 |
| Confusion matrix (tn, fp, fn, tp) | 19513, 714, 3249, 11827 |

Rebased precision matters more than the raw figure: at real-world prevalence, most URLs are legitimate, so even a low false-positive rate produces many false alarms per true phishing hit. The rebased number is the honest one to quote.

## 6. Calibration

Reliability curve on validation (predicted-probability bin mean vs. observed phishing rate in that bin; perfectly calibrated means the two columns match). Brier score: **0.0814**.

| Predicted mean | Observed rate |
|---|---|
| 0.033 | 0.030 |
| 0.078 | 0.059 |
| 0.137 | 0.116 |
| 0.156 | 0.141 |
| 0.180 | 0.149 |
| 0.229 | 0.182 |
| 0.534 | 0.597 |
| 0.993 | 0.996 |
| 1.000 | 1.000 |
| 1.000 | 1.000 |

No calibration layer is applied before export. This dataset's ~43% phishing prevalence is far from real traffic, so a calibrator fit here would calibrate probabilities *to this dataset*, not to production - it would not close the gap that matters. The prevalence rebasing in §5 addresses that gap directly instead.

## 7. Explainability (SHAP)

Mean |SHAP value| over a validation sample, in the model's native output units (log-odds for logistic regression and XGBoost; probability for random forest - SHAP explainer defaults differ by model type). Larger = more influence on the prediction, in either direction.

| Feature | Mean |SHAP value| |
|---|---|
| host_subdomain_depth | 1.0990 |
| host_length | 0.9969 |
| host_registered_domain_length | 0.6817 |
| host_num_digits | 0.6354 |
| host_tld_freq | 0.5772 |
| host_entropy | 0.4660 |
| host_longest_token_len | 0.2765 |
| host_num_dots | 0.2471 |
| host_num_hyphens | 0.0797 |
| host_digit_ratio | 0.0675 |
| host_keyword_hits | 0.0463 |
| host_brand_lookalike | 0.0035 |
| host_is_punycode | 0.0018 |
| host_port_nonstandard | 0.0000 |
| host_ip_obfuscated | 0.0000 |

## 8. Limitations

- Trained on one dataset (PhiUSIIL, see the data card) collected at one point in time; phishing URL patterns evolve, and this model will drift.
- URL-only: it has no access to page content, redirects, DNS, WHOIS or reputation data (those arrive in Stage 2+ as separate, non-ML evidence).
- The legitimate class here is entirely bare homepages; a legitimate URL with a path or query is out-of-distribution for this model in a way real traffic will not be. An external validation set with legitimate URLs that have paths would test this directly (tracked as follow-up work, not yet available).
- Probabilities reflect this dataset's ~43% phishing prevalence; see §5 and §6.
