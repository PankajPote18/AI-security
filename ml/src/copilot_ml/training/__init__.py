"""Training-only code: model definitions, the preprocessing+model pipeline, and the training
loop. Never imported by `inference` (checked by a test) — inference only loads the artifact
this package produced."""
